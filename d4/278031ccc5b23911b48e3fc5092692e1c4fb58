v1-->v2:
The previous RFC [06/55] is split into two patches. This is the first one.
Expanded sys_regs[] array instead of using a separate el2_regs[]
Fixed a bug; It was accessing sys_regs[] array for SP_EL1, which is saved in kvm_regs[].
